node_modules/
.expo/
dist/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# Native projects generated by `expo prebuild` / `expo run:ios|android`.
# This app uses Continuous Native Generation: app.json + plugins are the source
# of truth, so these dirs are disposable build output. Delete and regenerate
# freely with `expo prebuild --clean`.
/ios/
/android/
