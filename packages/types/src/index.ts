// This package is reserved for shared domain types that are truly global
// and not tied to a specific API service.
//
// Most types live with their API services in @dispatch/api (colocated pattern).
// Only add types here if they need to be shared across multiple packages
// or are foundational domain types used everywhere.

// Placeholder - currently all types are colocated with APIs
export {};
